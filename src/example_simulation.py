from BattleSimulation import BattleSimulation
from src.Cards.Red import RedStrike, RedDefend, Bash

cards = [   # Deck that the Actor will be playing with
    RedStrike(), RedStrike(), RedStrike(), RedStrike(), RedStrike,
    RedDefend(), RedDefend(), RedDefend(), RedDefend(),
    Bash()
]

